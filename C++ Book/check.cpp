#include <iostream>
using namespace std;

int main()
{
    // char array[] = "abc";

    // int * p = new int(2.3);
    // int * q = new int[4];

    // int *p;
    // float *q;

    // p = int*(q);
    // p = (int *)q;
    // // p = int(*q);
    // p = (int *)(q);

    // *p = 25;
    // *q = 4.5;
    // *p = *p + *q;

    // double x;
    // int m;
    // x = (double)m;
    // m = int(x);
    // x = true;
    // x = (double)(true);
    // m = m + false / true;

    // float z = (z == z);

    // int g = 8;
    // if(!(g%2 + 1 -1)){
    //     cout<<"sahi hai";
    // }
    // int f = !(g%2 + 1 -1);
    // cout<<f;

    // int x,y = 10,z = 10;
    // x = (y == z);
    // cout<<x;

    int x = 16;
    x = x >> 1;
    cout<<x;
    return 0;
}